<p id="notice"><%= notice %></p>

<h1>Oenologists</h1>

<table class="table">
  <thead class="thead-dark">
    <tr class="text-center">
      <th>Name</th>
      <th>Age</th>
      <th>Nationality</th>
      <th colspan="3">Actions</th>
    </tr>
  </thead>

  <tbody>
    <% @oenologists.order(:age).each do |oenologist| %>
      <tr>
        <td><%= oenologist.name %></td>
        <td><%= oenologist.age %></td>
        <td><%= oenologist.nationality %></td>
        <td><%= link_to 'Show', oenologist %></td>
        <td><%= link_to 'Edit', edit_oenologist_path(oenologist) %></td>
        <td><%= link_to 'Destroy', oenologist, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to '<button type="button" class="btn btn-dark">New Eonologists</button>'.html_safe, new_oenologist_path %>
<%= link_to '<button type="button" class="btn btn-dark">Wines List</button>'.html_safe, wines_path %>
<%= link_to '<button type="button" class="btn btn-dark">Strains List</button>'.html_safe, strains_path %>
<%= link_to '<button type="button" class="btn btn-dark">Eonologists List</button>'.html_safe, oenologists_path %>
<%= link_to '<button type="button" class="btn btn-dark">Jobs List</button>'.html_safe, job_titles_path %>
