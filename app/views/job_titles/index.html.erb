<p id="notice"><%= notice %></p>

<h1>Job Titles</h1>

<table class="table">
  <thead class="thead-dark">
    <tr class="text-center">
      <th>Name:</th>
      <th colspan="3">Actions</th>
    </tr>
  </thead>

  <tbody>
    <% @job_titles.each do |job_title| %>
      <tr class="text-center">
        <td><%= job_title.name %></td>
        <td><%= link_to 'Show', job_title %></td>
        <td><%= link_to 'Edit', edit_job_title_path(job_title) %></td>
        <td><%= link_to 'Destroy', job_title, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to '<button type="button" class="btn btn-dark">New Job</button>'.html_safe, new_job_title_path %>
<%= link_to '<button type="button" class="btn btn-dark">Wines list</button>'.html_safe, wines_path %>
<%= link_to '<button type="button" class="btn btn-dark">Strains List</button>'.html_safe, strains_path %>
<%= link_to '<button type="button" class="btn btn-dark">Oenologists List</button>'.html_safe, oenologists_path %>
<%= link_to '<button type="button" class="btn btn-dark">Magazines List</button>'.html_safe, magazines_path %>
